<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.fabrica.projeto.carteiravacinadigital">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".adapters.AdapterVacina" />
        <activity android:name=".PainelVacina"
            android:label="@string/title_activity_painel_pessoa"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".CadastroVacina" />
        <activity android:name=".CadastroPessoa" />
        <activity
            android:name=".PainelPessoaActivity"
            android:label="@string/title_activity_painel_pessoa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadastroPrincipal" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".Apresentacao"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>

</manifest>